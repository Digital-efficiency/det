// This file re-exports all components from the InfoCard directory.
// This allows users to import individual components from the InfoCard directory without having to specify the full path.
// For example, instead of writing `import InfoCard from './InfoCard/InfoCard';`, users can simply write `import { InfoCard } from './InfoCard';`.
export { default as InfoCard } from './InfoCard';